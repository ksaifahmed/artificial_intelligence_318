#include <bits/stdc++.h>
using namespace std;

bool is_solvable(int **matrix, int k) {
    //odd grid_size e.g. 3
    if(k%2) {
        return true;
    }

    //even grid_size e.g. 4
    else {
        return false;
    }
}

